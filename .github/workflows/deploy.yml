name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# GITHUB_TOKEN 에 Pages 배포 권한을 준다. 그 외는 읽기만.
permissions:
  contents: read
  pages: write
  id-token: write

# 배포는 겹치면 안 되므로 진행 중인 것을 취소하지 않고 큐에 세운다.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # v4 계열 액션은 러너에서 Node 20 으로 돌아가 deprecation 경고가 난다.
      # 현재 메이저로 올려 Node 24 런타임을 쓴다.
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          # Vite 8 은 Node 20 이상을 요구한다.
          node-version: '22'
          cache: npm

      - run: npm ci

      # 프로젝트 페이지는 https://<user>.github.io/<repo>/ 로 서빙되므로
      # 저장소 이름을 base 로 넣어야 에셋 경로가 맞는다.
      - name: Build
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
        run: npm run build

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
